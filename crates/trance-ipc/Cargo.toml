[package]
name = "trance-ipc"
version = "0.3.66"
edition = "2024"
description = "Out-of-process screensaver execution and shared memory IPC"
license = "Apache-2.0"

[dependencies]
libc = { workspace = true }
trance-api = { path = "../../trance-api" }

[dev-dependencies]
proptest = "1"
